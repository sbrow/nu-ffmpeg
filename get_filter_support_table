#!/usr/bin/env -S nu -n
# vim: filetype=nu :

use filters.nu;

def main [] {
  (
    help commands
    | where command_type == "custom" and name !~ "banner|p(rompt|wd)|^main$"
    | select name description
    | update description {
      str replace -a "\n" " "
    }
    | to md -p
  )
}
