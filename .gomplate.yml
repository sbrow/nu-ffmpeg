inputFiles:
  - README.md.tmpl
outputFiles:
  - README.md
plugins:
  filters: ./get_filter_support_table
  ffprobe: ./get_ffprobe_commands_table
  version: ./nu_version
  example:
    cmd: ./bin/run-example
    #args: []
#pluginTimeout: 10s

